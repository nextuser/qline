# javascript实现生词本数据结构
- 每个单词，具有一个分值，初始分值100
- 每次单词学习的时候，发生错误，分值加1
- 每次单词学习的时候，发生正确，分值减1
- 维护一个单词链表，按照分值排序，从高到低
- 背单词的时候，每次从链表头开始背
- 当单词分值小于95时，从链表中移除
- 数据结构应该很容易从尾部查找分值最低的单词，
- 这个链表应该是一个双向链表，方便删除，插入操作
- 可以添加新的单词到链表中，出事分值为100。
- 生成一系列测试代码。